const TesteProps = ({id, modelo, marca, ano}) =>{


    return <div>
        <ul>
            <li>
            {id} - {modelo} - {marca} - {ano}
            </li>
        </ul>
    </div>
}

export default TesteProps;